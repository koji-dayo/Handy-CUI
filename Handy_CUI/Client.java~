import java.io.*;
import java.net.*;

class Client{
    public static void main(String[] args){
	try{
	    Socket cocket = new Socket("192.168.0.13",5000);//サーバー側のIPアドレスとポート番号を指定してソケット接続します
	    BufferedReader reader = new BufferedReader(new InputStreamReader(socket.getInputStream()));//ストリーム作成
	    String message = reader.readLine();
	    System.out.println("頼まれたメニューは" + message);
	    reader.close();
	}catch(IOException e){
	    System.out.println(e);
	}
    }
}
